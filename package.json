{
    "name": "ubreakwefix-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build && mkdir -p ../public && cp -r dist/* ../public"
    },
    "engines": {
        "node": ">=18"
    }
}